import InventoryGroupingsDisplay from './InventoryGroupingsDisplay.vue';

new Vue({
    el: '#inventory-groupings',
    components : {
        'inventory-groupings-display': InventoryGroupingsDisplay,
    },
});